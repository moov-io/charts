name: OpenShift Charts

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    name: Test Charts
    runs-on: ubuntu-latest
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v2

      - name: Test
        run: make test

      - name: Setup
        run: make integration-setup

      - name: Integration
        run: make integration-openshift

      - name: Cleanup
        run: make integration-destroy
