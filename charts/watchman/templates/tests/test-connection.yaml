apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "watchman.fullname" . }}-test-connection"
  labels:
    {{- include "watchman.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "watchman.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
