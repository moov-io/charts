apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ach.fullname" . }}-test-connection"
  labels:
    {{- include "ach.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ach.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
